services:
  nginx:
    image: nginx:alpine
    ports:
      - "8084:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend

  backend:
    build: .
    volumes:
      - .:/app
      - ../../samples:/samples
    ports:
      - "8000:8000"
